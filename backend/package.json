{
  "name": "pornsearch-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browser": "npx puppeteer browsers install chrome",
    "install-puppeteer": "npm install puppeteer",
    "prestart": "mkdir -p downloads && npm run install-puppeteer"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pornsearch": "^2.4.0",
    "archiver": "^5.3.1",
    "axios": "^1.4.0",
    "@justalk/pornhub-api": "^1.0.1",
    "cheerio": "^1.0.0-rc.12",
    "puppeteer-core": "^21.1.0"
  },
  "devDependencies": {
    "puppeteer": "^21.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
